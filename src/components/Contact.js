import React from 'react'

function Contact(props) {
  return (
    <div>
      <h1 class ="App" > Contact {props.myinfo.name} for further query</h1>
    </div>
  )
}

export default Contact
